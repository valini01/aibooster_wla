name: Terraform Orchestrator

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Information
        run: |
          echo "🚀 Terraform Infrastructure Workflows"
          echo ""
          echo "Available workflows in Actions tab:"
          echo "1. terraform-init.yml - Initialize and validate Terraform (auto-runs on push)"
          echo "2. terraform-plan.yml - Plan infrastructure changes (manual trigger)"
          echo "3. terraform-apply.yml - Apply infrastructure changes (manual trigger)"
          echo "4. terraform-destroy.yml - Destroy infrastructure (manual trigger)"
          echo ""
          echo "💡 Note: The init workflow should have run automatically when you pushed to main."
          echo "💡 Use the Actions tab to manually trigger plan, apply, and destroy workflows."