name: "Test Workflow"

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Test Step
        run: echo "This is a test to verify GitHub Actions is working"
      
      - name: Check Repository
        run: |
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref }}"
          echo "Event: ${{ github.event_name }}"